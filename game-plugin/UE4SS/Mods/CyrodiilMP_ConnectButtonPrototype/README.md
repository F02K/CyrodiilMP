# CyrodiilMP Connect Button Prototype

This UE4SS mod is a placeholder for the first main-menu integration.

It currently registers the console command:

```text
cyro_connect
```

The command logs the intended server target. After the runtime inspector identifies the actual main-menu widget and button construction path, this prototype can be changed to inject or hook a visible `CyrodiilMP` / `Connect` button.
